// Ch10 User Input
// confirm() is synchronous - blocks execution until OK/Cancel is clicked
// message string is just display text, has zero effect on the return value

let userConfirm = confirm("Ok === True\nCancel === False");
console.log(userConfirm);
